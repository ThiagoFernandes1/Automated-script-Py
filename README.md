# Task Manager

Este é um projeto básico para criar a estrutura inicial de um gerenciador de tarefas.

## Estrutura do Projeto

A estrutura do projeto gerada pelo script `create_project.py` é a seguinte:


## Como Usar

1. Certifique-se de ter o Python instalado em sua máquina.
2. Execute o script `create_project.py` no terminal para gerar a estrutura do projeto:

   bash
   python create_project.py